import { CategoryFiltering } from "components";
import { View, Text, ScrollView } from "react-native";

export const CategoryFilterScreen = () => {
  return (
    <ScrollView>
      <CategoryFiltering />
    </ScrollView>
  );
};
